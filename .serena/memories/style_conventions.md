# Style Conventions
- Prefer strict typing in Python; use modern built-in type annotations (`list`, `dict`).
- Linting/formatting via ruff + black.
- Use list comprehensions and match-case where they improve readability.
- Use Typer for CLI code; tests with pytest.
- Follow AGENTS.md repo instructions for language-specific patterns.